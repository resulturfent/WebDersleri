<!--
    table template
    https://bootsnipp.com/snippets/P2eO9
-->
@using DLL.EntitiesTablolar

@model List<Kategoriler>

<div class="row">
    <div class="col-lg-12 col-md-12 ">
        <div class="row">
            <div class="col-10">
                <h4><small>Kategori İşlemleri</small> </h4>
            </div>
            <div class="col-2">
                <a asp-action="Ekle" asp-controller="Kategori" class="btn btn-sm btn-success">Ekle</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th class="text-center">#</th>
                        <th>Kategori Adı</th>
                        <th>Ürün Sayısı</th>
                        <th>İşlem</th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-center">@item.Id</td>
                            <td>@item.Adi</td>
                            <td class="text-right">0</td>
                            <td class="td-actions text-right">
                                <a asp-action="Guncelle" asp-controller="Kategori" class="btn  btn-sm"  asp-route-id="@item.Id">
                                    <i class="material-icons text-primary">edit</i></a>
                                <a asp-action="Sil" asp-controller="Kategori" asp-route-id="@item.Id" class="btn btn-sm">
                                     <i class="material-icons text-danger">delete</i>
                                </a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>

   


    
   
